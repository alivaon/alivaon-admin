{
  "name": "alivaon-admin",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm --filter @alivaon/admin run build",
    "lint": "pnpm -r run lint",
    "api:generate": "pnpm --filter @alivaon/api-client run generate"
  }
}
